# 🎯 Trading Alert System - Быстрая справка

## Три способа управления сигналами

### 1️⃣ Gradio Web UI (Рекомендуется для новичков)

**Запуск:**
```bash
python gradio_app.py
# или двойной клик на
run_gradio.bat
```

**URL:** http://localhost:7860

**Что можно делать:**
- ✅ Создавать сигналы через удобную форму
- ✅ Просматривать все активные сигналы
- ✅ Удалять сигналы по ID
- ✅ Проверять текущую цену на бирже
- ✅ Синхронизировать из Google Sheets

---

### 2️⃣ Google Sheets (Для админов и быстрых правок)

**Формат таблицы:**

| symbol   | condition | target_price | exchange | active | pushover_user_key | notes      |
|----------|-----------|--------------|----------|--------|-------------------|------------|
| BTCUSDT  | above     | 50000        | bybit    | TRUE   | user_xyz123       | Bitcoin    |
| ETHUSDT  | below     | 3000         | binance  | TRUE   | user_xyz123       | Ethereum   |
| SOLUSDT  | above     | 100          | coinbase | TRUE   | user_abc456       | Solana     |

**Как использовать:**
1. Откройте вашу Google Таблицу
2. Добавьте/измените строки
3. Откройте Gradio → вкладка "Sync from Sheets"
4. Нажмите "Sync from Google Sheets"
5. ✅ Данные загружены в DynamoDB

---

### 3️⃣ AWS Lambda (Автоматизация 24/7)

**Что делает:**
- Запускается каждые 5 минут по расписанию CloudWatch
- Читает сигналы из DynamoDB
- Проверяет цены на биржах
- Отправляет уведомления через Pushover

**Как работает:**
```
CloudWatch Event (каждые 5 мин)
         ↓
    AWS Lambda
         ↓
   Читает DynamoDB
         ↓
  Проверяет цены на биржах
         ↓
  Отправляет Pushover уведомления
```

---

## 📊 Сравнение методов

| Метод | Когда использовать | Преимущества | Недостатки |
|-------|-------------------|-------------|-----------|
| **Gradio UI** | Создание 1-10 сигналов, просмотр, управление | Удобно, интуитивно, не нужны технические знания | Нужно держать открытым браузер |
| **Google Sheets** | Массовое редактирование, быстрые правки | Копировать/вставить, Excel-like | Нужна синхронизация в Gradio |
| **AWS Lambda** | Автоматические проверки 24/7 | Не требует вашего участия | Нужен AWS аккаунт, деплоймент |

---

## 🚀 Типичные сценарии использования

### Сценарий 1: Создать 1-2 сигнала (новичок)

**Используйте: Gradio UI**

1. Запустите: `python gradio_app.py`
2. Откройте: http://localhost:7860
3. Вкладка "Create Signal"
4. Заполните форму:
   - Signal Name: `BTC Alert`
   - Exchange: `bybit`
   - Symbol: `BTCUSDT`
   - Condition: `above`
   - Target Price: `50000`
5. Нажмите "Create Signal"
6. ✅ Сигнал создан и сохранен в DynamoDB

---

### Сценарий 2: Создать 50+ сигналов (опытный пользователь)

**Используйте: Google Sheets**

1. Откройте Google Таблицу
2. Скопируйте/вставьте ваш список символов:
   ```
   BTCUSDT  above  50000  bybit   TRUE
   ETHUSDT  below  3000   binance TRUE
   SOLUSDT  above  100    bybit   TRUE
   ... (50 строк)
   ```
3. Откройте Gradio → вкладка "Sync from Sheets"
4. Нажмите "Sync from Google Sheets"
5. ✅ Все 50 сигналов загружены в DynamoDB

---

### Сценарий 3: Автоматизировать проверку 24/7

**Используйте: AWS Lambda**

1. Создайте сигналы через Gradio или Sheets
2. Задеплойте Lambda функцию (см. DEPLOY_AWS_LAMBDA.md)
3. Настройте CloudWatch расписание (каждые 5 минут)
4. ✅ Lambda автоматически:
   - Читает ваши сигналы из DynamoDB
   - Проверяет цены на биржах
   - Отправляет Pushover уведомления

---

### Сценарий 4: Быстро изменить целевую цену

**Используйте: Google Sheets**

1. Откройте таблицу
2. Найдите строку с нужным символом
3. Измените `target_price` (например: `50000` → `55000`)
4. Откройте Gradio → вкладка "Sync from Sheets"
5. Нажмите "Sync from Google Sheets"
6. ✅ Цена обновлена в DynamoDB

---

### Сценарий 5: Проверить текущую цену

**Используйте: Gradio UI**

1. Откройте http://localhost:7860
2. Вкладка "Check Price"
3. Выберите Exchange: `bybit`
4. Введите Symbol: `BTCUSDT`
5. Нажмите "Check Price"
6. Получите:
   ```
   ✅ Current Price Data:
   📊 Symbol: BTCUSDT
   💱 Exchange: bybit
   💰 Price: $49,823.45
   📈 24h Volume: $2,345,678,901.23
   ⏰ Time: 2025-11-14 15:30:45
   ```

---

### Сценарий 6: Удалить ненужный сигнал

**Используйте: Gradio UI**

1. Вкладка "View Signals"
2. Найдите сигнал, скопируйте короткий ID (например: `a1b2c3d4...`)
3. Вкладка "Delete Signal"
4. Вставьте ID: `a1b2c3d4...`
5. Нажмите "Delete Signal"
6. ✅ Сигнал удален из DynamoDB

---

## 🔄 Workflow: Как всё работает вместе

```
1. СОЗДАНИЕ СИГНАЛОВ:
   
   Gradio UI → DynamoDB
              ↓
   Google Sheets → Sync → DynamoDB

2. АВТОМАТИЧЕСКАЯ ПРОВЕРКА:
   
   CloudWatch Event (каждые 5 мин)
              ↓
         AWS Lambda
              ↓
     Читает из DynamoDB
              ↓
   Проверяет цены на биржах
              ↓
    Если условие выполнено:
    Отправляет Pushover уведомление

3. ПРОСМОТР И УПРАВЛЕНИЕ:
   
   Gradio UI → Читает из DynamoDB
   Google Sheets → Редактируете → Sync → DynamoDB
```

---

## 💡 Лучшие практики

### ✅ DO (Делайте так):
- Используйте **Gradio** для создания 1-10 сигналов
- Используйте **Google Sheets** для массовых правок (10+)
- Используйте **Lambda** для автоматизации 24/7
- Храните все важные сигналы в **DynamoDB** (единый источник данных)
- Синхронизируйте Sheets в DynamoDB регулярно
- Проверяйте логи CloudWatch для отладки Lambda

### ❌ DON'T (Не делайте так):
- Не редактируйте сигналы только в Sheets без синхронизации
- Не храните критичные данные только в Sheets
- Не забывайте про географические ограничения (US регионы блокируют биржи)
- Не коммитьте `.env` в Git
- Не используйте один и тот же `pushover_user_key` для всех сигналов (если хотите разные уведомления)

---

## 🛠️ Быстрые команды

```bash
# Запустить Gradio UI
python gradio_app.py
# или
run_gradio.bat

# Простая проверка (без Lambda)
python simple_alert.py

# Проверка с Google Sheets
python simple_sheets_alert.py

# Собрать Lambda пакет
python build_lambda_package.py

# Запустить тесты
pytest tests/
```

---

## 📞 Нужна помощь?

1. **Gradio не запускается?** → См. GRADIO_GUIDE.md раздел "Troubleshooting"
2. **Lambda не работает?** → См. DEPLOY_AWS_LAMBDA.md раздел "Troubleshooting"
3. **Sheets не синхронизируется?** → Проверьте доступ Service Account к таблице
4. **Биржи недоступны?** → Проверьте AWS регион (должен быть НЕ US)

---

## 🎯 Итоговая рекомендация

**Для максимальной гибкости используйте ВСЁ ВМЕСТЕ:**

1. **Создавайте сигналы:** Gradio UI (удобно) или Google Sheets (быстро)
2. **Храните данные:** DynamoDB (единый источник)
3. **Автоматизируйте:** AWS Lambda (24/7 проверка)
4. **Управляйте:** Выбирайте интерфейс по ситуации

**Это архитектура Варианта 3 - максимальная гибкость!** 🚀

---

**Happy Trading! 📈**
