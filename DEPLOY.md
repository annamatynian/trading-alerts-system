# 🚀 Деплой на Render.com

## Быстрый старт (5 минут)

### 1. Подготовьте GitHub репозиторий

Если у вас еще нет Git репозитория:

```bash
cd trading_alert_system
git init
git add .
git commit -m "Initial commit"
```

Создайте репозиторий на GitHub и загрузите код:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/trading-alert-bot.git
git branch -M main
git push -u origin main
```

### 2. Зарегистрируйтесь на Render.com

1. Перейдите на https://render.com
2. Нажмите "Get Started" (бесплатно)
3. Войдите через GitHub

### 3. Создайте новый сервис

1. На дашборде Render нажмите **"New +"** → **"Background Worker"**
2. Подключите ваш GitHub репозиторий
3. Render автоматически обнаружит `render.yaml` ✅

### 4. Добавьте переменные окружения

В настройках сервиса добавьте:

| Переменная | Значение |
|------------|----------|
| `TELEGRAM_BOT_TOKEN` | Ваш токен от @BotFather |
| `TELEGRAM_CHAT_ID` | Ваш chat ID |
| `BYBIT_API_KEY` | (опционально) |
| `BYBIT_API_SECRET` | (опционально) |
| `BINANCE_API_KEY` | (опционально) |
| `BINANCE_API_SECRET` | (опционально) |

**Остальные переменные уже заданы в render.yaml!**

### 5. Деплой!

Нажмите **"Create Web Service"** и подождите 2-3 минуты.

Готово! 🎉 Ваш бот работает 24/7 бесплатно!

## 📊 Мониторинг

- Логи доступны в разделе "Logs" на Render.com
- Бот будет проверять алерты каждый час
- Если бот "засыпает" - он автоматически проснется при следующей проверке

## 🔄 Обновление бота

Просто сделайте `git push` в репозиторий - Render автоматически задеплоит изменения!

```bash
git add .
git commit -m "Update bot"
git push
```

## ⚙️ Изменение интервала проверки

В Render.com → Environment → измените `CHECK_INTERVAL_SECONDS`:
- `300` = 5 минут
- `900` = 15 минут
- `3600` = 1 час (по умолчанию)

## 💡 Советы

1. **Бесплатный план:** 750 часов/месяц (достаточно для 24/7)
2. **Сон после 15 минут:** Не проблема - бот проснется
3. **Логи:** Смотрите в реальном времени в Render dashboard

## 🆘 Проблемы?

Если бот не запускается:
1. Проверьте логи в Render
2. Убедитесь что все переменные окружения заданы
3. Проверьте что `TELEGRAM_BOT_TOKEN` правильный
