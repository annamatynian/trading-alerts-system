# 🔧 Настройка прокси для обхода геоблокировки Binance

## Проблема

Если вы видите эту ошибку в логах:
```
Service unavailable from a restricted location according to 'b. Eligibility' 
in https://www.binance.com/en/terms
```

**Причина:** Binance блокирует доступ с серверов, расположенных в определенных регионах (США, Япония и др.).

## Быстрое решение (5 минут)

### Шаг 1: Получите прокси-сервер

Выберите один из провайдеров:

| Провайдер | Преимущества | Ссылка |
|-----------|-------------|--------|
| **Webshare.io** | 🆓 Бесплатный план (10 прокси) | https://www.webshare.io |
| **Smartproxy** | ⭐ Надежный, резидентские прокси | https://smartproxy.com |
| **Proxy-Cheap** | 💰 Недорогой вариант | https://www.proxy-cheap.com |
| **Bright Data** | 🏢 Профессиональный | https://brightdata.com |

**Важно:** Выбирайте прокси из разрешенных стран:
- 🇨🇿 Чехия
- 🇵🇱 Польша
- 🇩🇪 Германия
- 🇨🇭 Швейцария
- 🇨🇴 Колумбия
- 🇳🇱 Нидерланды

### Шаг 2: Получите данные прокси

После регистрации у провайдера вы получите:
- IP-адрес: например `45.123.45.67`
- Порт: например `8080`
- Логин: например `myuser` (если требуется)
- Пароль: например `mypassword` (если требуется)

### Шаг 3: Добавьте переменную окружения

#### Формат PROXY_URL:

**С аутентификацией:**
```
http://логин:пароль@ip:порт
```

**Без аутентификации:**
```
http://ip:порт
```

#### Примеры:

**С логином и паролем:**
```
http://myuser:mypassword@45.123.45.67:8080
```

**Без логина:**
```
http://45.123.45.67:8080
```

**HTTPS прокси:**
```
https://myuser:mypassword@45.123.45.67:8443
```

### Шаг 4: Настройка в Leapcell

1. Откройте ваш проект в Leapcell Dashboard
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте новую переменную:
   - **Key:** `PROXY_URL`
   - **Value:** ваш прокси URL (из Шага 3)
4. Нажмите **Save** или **Add**
5. Нажмите **Redeploy** или **Restart**

### Шаг 5: Проверка

1. Откройте **Logs** в Leapcell Dashboard
2. Найдите строку: ✅ `Using proxy for Binance connection`
3. Найдите строку: ✅ `Connected to Binance mainnet`
4. НЕ должно быть: ❌ `Service unavailable from a restricted location`

## Тестирование локально

Чтобы протестировать прокси перед деплоем:

1. Добавьте `PROXY_URL` в ваш `.env` файл:
   ```env
   PROXY_URL=http://user:pass@ip:port
   ```

2. Запустите бота локально:
   ```bash
   python src/main.py
   ```

3. Проверьте логи на успешное подключение

## Проверка прокси вручную

Простой Python скрипт для проверки работы прокси:

```python
import requests

# Замените на ваши данные
PROXY_URL = "http://user:pass@45.123.45.67:8080"

proxies = {
    'http': PROXY_URL,
    'https': PROXY_URL
}

try:
    # Проверяем доступ к Binance API
    response = requests.get(
        'https://api.binance.com/api/v3/ping',
        proxies=proxies,
        timeout=10
    )
    
    if response.status_code == 200:
        print("✅ Прокси работает! Binance доступен!")
        print(f"Response: {response.json()}")
    else:
        print(f"❌ Ошибка: {response.status_code}")
        
except Exception as e:
    print(f"❌ Не удалось подключиться: {e}")
```

## Часто задаваемые вопросы

### Бесплатные прокси работают?

⚠️ Не рекомендуется. Бесплатные публичные прокси:
- Ненадежны и часто падают
- Медленные
- Могут быть уже заблокированы Binance
- Небезопасны для передачи данных

Лучше использовать бесплатный план Webshare.io.

### Нужен ли прокси для Bybit?

Нет, Bybit не имеет таких жестких геоограничений. Прокси нужен только для Binance.

### Можно ли использовать VPN вместо прокси?

Нет. VPN работает на уровне операционной системы, а ваш бот работает на сервере Leapcell, где вы не контролируете сетевые настройки. Нужен именно HTTP/HTTPS прокси в коде.

### Влияет ли прокси на скорость работы?

Минимально. Задержка составляет обычно 50-200мс дополнительно. Для бота, который проверяет цены раз в час, это незаметно.

### Безопасно ли передавать API ключи через прокси?

Да, если:
1. Используете HTTPS прокси (шифрование)
2. Используете надежного провайдера
3. Ваши API ключи имеют только права на чтение

**Рекомендация:** Создавайте API ключи только с правами на чтение данных (read-only).

## Устранение проблем

### Прокси не работает

1. **Проверьте формат URL:**
   - Должно быть `http://` или `https://` в начале
   - Специальные символы в пароле должны быть закодированы (URL encoding)
   
2. **Проверьте логин и пароль:**
   - Нет лишних пробелов
   - Правильный регистр символов
   
3. **Проверьте IP и порт:**
   - IP должен быть доступен извне
   - Порт открыт

### Ошибка "Proxy Authentication Required"

- Проверьте логин и пароль в PROXY_URL
- Убедитесь, что у прокси не истек срок действия

### Ошибка "Connection timeout"

- Прокси-сервер недоступен или перегружен
- Попробуйте другой прокси из списка провайдера
- Увеличьте timeout в коде (по умолчанию 10 секунд)

## Дополнительно

### Альтернативные решения

Если прокси не подходит, рассмотрите:

1. **Другой хостинг:**
   - Railway.app
   - Render.com (выбрать регион Singapore/Frankfurt)
   - DigitalOcean (выбрать дата-центр в Европе)

2. **VPS в правильном регионе:**
   - Купить VPS в разрешенной стране
   - Установить бота напрямую

### Мониторинг работы прокси

Добавьте в код логирование для отслеживания проблем:

```python
logger.info(f"Proxy status: {os.getenv('PROXY_URL', 'Not configured')}")
```

---

**Готово!** После настройки прокси ваш бот должен успешно подключаться к Binance из любого региона. 🎉
