# 🔄 Как наши изменения в GitHub влияют на все компоненты системы

## 📊 Общая картина взаимодействия компонентов

```
┌─────────────────────────────────────────────────────────────────────┐
│                           GITHUB REPOSITORY                          │
│                    (наши изменения закоммичены здесь)                │
└────────────┬──────────────┬──────────────┬──────────────┬───────────┘
             │              │              │              │
             │              │              │              │
    ┌────────▼─────┐  ┌────▼────┐  ┌──────▼──────┐  ┌───▼────────┐
    │ Lambda Code  │  │ Gradio  │  │ Data Models │  │ Test Data  │
    │ (Python)     │  │ app.py  │  │ (signal.py) │  │ (scripts)  │
    └────────┬─────┘  └────┬────┘  └──────┬──────┘  └───┬────────┘
             │              │              │             │
             └──────┬───────┴──────┬───────┴─────────────┘
                    │              │
         ┌──────────▼──────────────▼──────────┐
         │      DynamoDB Table                │  ◄──── ЦЕНТРАЛЬНОЕ ХРАНИЛИЩЕ
         │   trading-signals-eu (eu-west-1)  │        (с User ID полем!)
         │                                    │
         │  ┌──────────────────────────────┐ │
         │  │ signal_id (Sort Key)         │ │
         │  │ user_id   (NEW! для фильтра) │ │  ◄─── НАШЕ ИЗМЕНЕНИЕ
         │  │ name                          │ │
         │  │ exchange (binance/bybit...)   │ │
         │  │ symbol (BTCUSDT...)           │ │
         │  │ target_price                  │ │
         │  │ triggered_count               │ │
         │  └──────────────────────────────┘ │
         └──────────┬───────────┬─────────────┘
                    │           │
      ┌─────────────┘           └─────────────┐
      │                                       │
      ▼                                       ▼
┌──────────────────┐                   ┌──────────────────┐
│   AWS Lambda     │                   │  Gradio UI       │
│  (Cron каждый    │                   │ (Hugging Face    │
│   час)           │                   │  Spaces)         │
│                  │                   │                  │
│ 1. Читает        │                   │ 1. Создание      │
│    сигналы       │                   │    сигнала с     │
│ 2. Проверяет     │                   │    User ID       │
│    цены          │                   │ 2. Фильтр по     │
│ 3. Отправляет    │                   │    User ID       │
│    уведомления   │                   │ 3. Просмотр      │
└─────┬─────┬──────┘                   └────┬─────────────┘
      │     │                               │
      │     └──────────┐                    │
      │                │                    │
      ▼                ▼                    ▼
┌──────────┐    ┌─────────────┐    ┌──────────────┐
│ Binance  │    │  Pushover   │    │ Google       │
│ Bybit    │    │             │    │ Sheets       │
│ Coinbase │    │ (отправка   │    │ (опционально)│
│          │    │  на телефон)│    │              │
│ (цены)   │    └─────────────┘    └──────────────┘
└──────────┘
```

---

## 🎯 ВЛИЯНИЕ НАШИХ MULTIUSER ИЗМЕНЕНИЙ

### 1️⃣ **GitHub → DynamoDB**

**Что изменили в GitHub:**
```python
# src/models/signal.py
class SignalTarget(BaseModel):
    user_id: Optional[str] = Field(None, description="User identifier")  # ◄── НОВОЕ ПОЛЕ
```

**Как это влияет на DynamoDB:**
- При создании сигнала через Gradio → сохраняется `user_id` в DynamoDB
- Каждый сигнал теперь привязан к пользователю
- Структура записи в DynamoDB:
  ```json
  {
    "signal_id": "abc123...",
    "user_id": "anna",          // ◄── НОВОЕ!
    "name": "BTC Alert",
    "exchange": "binance",
    "symbol": "BTCUSDT",
    "target_price": 95000
  }
  ```

---

### 2️⃣ **GitHub → Gradio (Hugging Face)**

**Что изменили в GitHub:**
```python
# app.py
# 1. User ID обязателен
if not user_id or user_id.strip() == "":
    return "❌ User ID is required!"

# 2. Фильтр по User ID
def get_signals_table(user_id: str = ""):
    signals = [s for s in signals if s.user_id == user_id]
```

**Как деплоится на Hugging Face:**
1. Вы делаете `git push` → код попадает в GitHub
2. На Hugging Face Spaces вы синхронизируете с GitHub
3. Hugging Face запускает `app.py` с новыми изменениями
4. Пользователи видят:
   - Обязательное поле "User ID (Required)"
   - Кнопку фильтра по User ID
   - Колонку "User ID" в таблице сигналов

---

### 3️⃣ **GitHub → AWS Lambda**

**Текущее состояние Lambda:**
```python
# lambda_function.py (строка ~80)
async def check_signals_from_sheets():
    # 1. Читает сигналы из DynamoDB
    signals = await storage.get_all_signals()

    # 2. Проверяет цены на биржах
    price = await exchange.get_price(signal.symbol)

    # 3. Если триггер сработал
    if condition_met:
        # Отправляет Pushover уведомление
        await notification_service.send(signal)
```

**ЧТО НУЖНО БУДЕТ ИЗМЕНИТЬ для multiuser:**
```python
# В будущем Lambda нужно будет модифицировать:
async def send_notification_to_user(signal):
    # Берём user_id из сигнала
    user_id = signal.user_id  # "anna" или "tomas"

    # Получаем Pushover ключ этого пользователя
    pushover_key = get_user_pushover_key(user_id)

    # Отправляем на телефон именно этого пользователя
    await send_pushover(pushover_key, signal)
```

**Где хранить mapping пользователей:**
```json
// AWS Secrets Manager
{
  "users": {
    "anna": {
      "pushover_user_key": "u123abc...",
      "pushover_device": "iPhone"
    },
    "tomas": {
      "pushover_user_key": "u456def...",
      "pushover_device": "Android"
    }
  }
}
```

---

### 4️⃣ **GitHub → Google Sheets**

**Текущая структура Sheets:**
| symbol   | condition | target_price | exchange | active | pushover_user_key | notes |
|----------|-----------|--------------|----------|--------|-------------------|-------|
| BTCUSDT  | above     | 50000        | bybit    | TRUE   | u123abc           | BTC   |

**Что нужно добавить для multiuser:**
| symbol   | condition | target_price | exchange | active | **user_id** | pushover_user_key | notes |
|----------|-----------|--------------|----------|--------|-------------|-------------------|-------|
| BTCUSDT  | above     | 50000        | bybit    | TRUE   | **anna**    | u123abc           | BTC   |
| ETHUSDT  | below     | 3000         | binance  | TRUE   | **tomas**   | u456def           | ETH   |

**Как это работает:**
1. Админ редактирует Google Sheets (добавляет `user_id`)
2. В Gradio нажимает "Sync from Sheets"
3. Скрипт читает Sheets → создаёт SignalTarget с `user_id` → сохраняет в DynamoDB

---

### 5️⃣ **GitHub → Pushover (уведомления на телефон)**

**Текущий поток:**
```
Lambda → Pushover API → Телефон (один и тот же для всех)
```

**Новый multiuser поток:**
```
Lambda → читает user_id из сигнала
       ↓
       Получает pushover_key для этого user_id
       ↓
       Pushover API (с правильным ключом)
       ↓
       Телефон Anna (если user_id=anna)
       или
       Телефон Tomas (если user_id=tomas)
```

**Пример кода для Lambda:**
```python
# lambda_function.py (будущее изменение)
async def send_notification(signal):
    # Получаем user_id из сигнала
    user_id = signal.user_id or "default"

    # Получаем конфигурацию пользователя из Secrets Manager
    user_config = await get_user_config(user_id)
    pushover_key = user_config["pushover_user_key"]

    # Отправляем именно этому пользователю
    await pushover.send(
        user_key=pushover_key,
        message=f"🚨 {signal.name}: {signal.symbol} reached ${signal.target_price}"
    )
```

---

## 📦 ПОЛНЫЙ ЦИКЛ: От коммита до уведомления на телефон

### Сценарий: Anna создаёт сигнал на BTC $95k

```
1. Anna открывает Gradio UI (на Hugging Face)
   ↓
2. Заполняет форму:
   - Signal Name: "BTC Moon"
   - User ID: "anna"              ◄── НАШЕ ИЗМЕНЕНИЕ
   - Symbol: BTCUSDT
   - Target Price: 95000
   - Exchange: binance
   ↓
3. Нажимает "Create Signal"
   ↓
4. Gradio (app.py) → Валидирует user_id
   ↓
5. Создаёт SignalTarget объект с user_id="anna"
   ↓
6. Сохраняет в DynamoDB:
   {
     "signal_id": "signal#abc123",
     "user_id": "anna",           ◄── НАШЕ ИЗМЕНЕНИЕ
     "name": "BTC Moon",
     "symbol": "BTCUSDT",
     "target_price": 95000,
     "exchange": "binance"
   }
   ↓
7. AWS Lambda запускается каждый час (CloudWatch cron)
   ↓
8. Lambda читает все сигналы из DynamoDB
   ↓
9. Находит сигнал Anna с user_id="anna"
   ↓
10. Проверяет цену BTC на Binance
    ↓
11. Если цена >= $95000:
    ↓
12. Lambda получает Pushover ключ для user_id="anna" из Secrets Manager
    ↓
13. Отправляет Pushover уведомление на телефон Anna:
    "🚨 BTC Moon: BTCUSDT reached $95000!"
    ↓
14. Телефон Anna получает push-уведомление! 📱🔔
```

---

## 🎨 Что уже работает VS что нужно доработать

### ✅ **Уже работает (закоммичено в GitHub):**
- ✅ User ID как обязательное поле в Gradio
- ✅ Фильтр по User ID в интерфейсе
- ✅ Сохранение user_id в DynamoDB
- ✅ Модель данных SignalTarget с полем user_id
- ✅ Скрипты генерации тестовых данных (anna, tomas)
- ✅ Документация изменений

### 🔧 **Что нужно доработать:**

#### 1. **AWS Lambda** (отправка на разные телефоны)
```python
# TODO: Добавить в lambda_function.py
# Чтение user_id из сигнала
# Получение Pushover ключа для каждого пользователя
# Отправка на правильный телефон
```

#### 2. **AWS Secrets Manager** (mapping пользователей)
```json
// TODO: Создать секрет user-pushover-mapping
{
  "users": {
    "anna": {"pushover_user_key": "..."},
    "tomas": {"pushover_user_key": "..."}
  }
}
```

#### 3. **Google Sheets** (добавить колонку user_id)
```
TODO: В Google Sheets добавить колонку "user_id"
```

#### 4. **Hugging Face Spaces** (синхронизация)
```
TODO: Синхронизировать код из GitHub на Hugging Face
```

---

## 🚀 План развёртывания

### Этап 1: Локальное тестирование (уже сделано ✅)
1. ✅ Код с user_id закоммичен
2. ✅ Скрипты тестовых данных созданы
3. ✅ Документация готова

### Этап 2: Hugging Face Spaces
1. Синхронизировать GitHub → Hugging Face
2. Перезапустить Space
3. Протестировать фильтр

### Этап 3: AWS Lambda
1. Обновить lambda_function.py (добавить логику user_id)
2. Создать Secrets Manager mapping
3. Деплой Lambda
4. Тест уведомлений

### Этап 4: Google Sheets (опционально)
1. Добавить колонку user_id
2. Обновить sync скрипт
3. Тест синхронизации

---

## 📞 Контакты компонентов

| Компонент | Где живёт | Зависимости | Использует user_id |
|-----------|-----------|-------------|-------------------|
| **GitHub** | github.com/annamatynian/trading-alerts-system | - | Хранит код ✅ |
| **DynamoDB** | AWS eu-west-1 | - | Хранит данные ✅ |
| **Gradio UI** | Hugging Face Spaces | GitHub, DynamoDB | Создание + Фильтр ✅ |
| **Lambda** | AWS eu-west-1 | DynamoDB, Биржи, Pushover | Читает (нужно доработать) 🔧 |
| **Pushover** | pushover.net | - | Нужна поддержка multi-user 🔧 |
| **Google Sheets** | Google Drive | - | Нужна колонка user_id 🔧 |

---

## 🎯 Главный вывод

**Наши изменения в GitHub влияют на ВСЕ компоненты:**

1. **DynamoDB** — получил новое поле `user_id`
2. **Gradio** — получил фильтр и валидацию
3. **Lambda** — нужно обновить для multi-user уведомлений
4. **Pushover** — нужен mapping пользователь → ключ
5. **Google Sheets** — нужна дополнительная колонка

**GitHub — это центр управления всей системой!** 🎮

Каждый `git push` обновляет код, который затем используется:
- Lambda для автоматических проверок
- Hugging Face для веб-интерфейса
- DynamoDB для хранения данных
- И все остальные компоненты!
