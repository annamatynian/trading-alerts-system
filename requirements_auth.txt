# JWT Authentication System Dependencies
# Install: pip install -r requirements_auth.txt

# Core dependencies
PyJWT>=2.8.0          # JWT token generation and validation
bcrypt>=4.0.0         # Secure password hashing
boto3>=1.34.0         # AWS DynamoDB for session storage

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
