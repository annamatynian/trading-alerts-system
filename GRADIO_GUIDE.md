# 🎨 Gradio Web Interface для Trading Alert System

## 📋 Обзор

Gradio интерфейс предоставляет удобный веб-интерфейс для управления торговыми сигналами, работая **одновременно** с DynamoDB и Google Sheets.

```
                  ↗ Google Sheets (для админов)
DynamoDB ← Lambda
                  ↖ Gradio UI (для пользователей)
```

## ✨ Возможности

### 1. **Create Signal** - Создание новых сигналов
- Создавайте сигналы через удобную форму
- Автоматическое сохранение в DynamoDB
- Опционально синхронизация с Google Sheets
- Поля:
  - Signal Name (название)
  - Exchange (биржа: Binance, Bybit, Coinbase)
  - Symbol (пара: BTCUSDT, ETHUSDT, и т.д.)
  - Condition (условие: above/below)
  - Target Price (целевая цена)
  - User ID (для уведомлений)
  - Notes (заметки)

### 2. **View Signals** - Просмотр всех сигналов
- Красивая таблица со всеми сигналами из DynamoDB
- Показывает: ID, Name, Exchange, Symbol, Condition, Target Price, Status, Created, Triggered Count
- Кнопка обновления для свежих данных

### 3. **Delete Signal** - Удаление сигналов
- Удаление по короткому ID (первые 8 символов)
- Автоматическое обновление таблицы после удаления

### 4. **Check Price** - Проверка цены
- Получение текущей цены с биржи
- Показывает: цену, 24h объем, время
- Поддержка всех подключенных бирж

### 5. **Sync from Sheets** - Синхронизация из Google Sheets
- Массовая загрузка сигналов из Google Sheets в DynamoDB
- Upsert логика (обновление существующих)
- Полезно для миграции данных

## 🚀 Быстрый старт

### Способ 1: Через BAT файл (Windows)
```bash
# Просто запустите
run_gradio.bat
```

### Способ 2: Вручную
```bash
# Активируйте виртуальное окружение
venv\Scripts\activate

# Установите зависимости (если еще не установлены)
pip install gradio pandas

# Запустите Gradio
python gradio_app.py
```

Интерфейс откроется по адресу: **http://localhost:7860**

## 📸 Скриншоты функционала

### Tab 1: Create Signal
```
┌─────────────────────────────────────────────────┐
│  📝 Create Signal                                │
├─────────────────────────────────────────────────┤
│  Signal Name: [My BTC Alert_____________]        │
│  Exchange:    [bybit ▼]                          │
│  Symbol:      [BTCUSDT______________]            │
│  Condition:   [above ▼]                          │
│  Target Price:[50000________________]            │
│  User ID:     [________________________]         │
│  Notes:       [________________________]         │
│               [________________________]         │
│  ☑ Also save to Google Sheets                   │
│                                                   │
│  [      Create Signal       ]                    │
└─────────────────────────────────────────────────┘
```

### Tab 2: View Signals
```
┌─────────────────────────────────────────────────┐
│  📊 View Signals                                 │
├─────────────────────────────────────────────────┤
│  [🔄 Refresh]                                    │
│                                                   │
│  ┌──────────────────────────────────────────┐   │
│  │ ID       │ Name    │ Exchange │ Symbol   │   │
│  │──────────┼─────────┼──────────┼──────────│   │
│  │ a1b2c3d4 │ BTC>50k │ bybit    │ BTCUSDT  │   │
│  │ e5f6g7h8 │ ETH<3k  │ binance  │ ETHUSDT  │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

## 🔧 Конфигурация

Gradio использует те же переменные окружения, что и Lambda:

### Обязательные переменные (.env)
```bash
# DynamoDB
DYNAMODB_TABLE_NAME=trading-alerts
DYNAMODB_REGION=us-east-2

# Google Sheets
GOOGLE_SHEETS_CREDENTIALS_PATH=./secret-medium-476300-m9-c141e07c30ad.json
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id

# Exchange API Keys (хотя бы одна биржа)
BINANCE_API_KEY=your_binance_key
BINANCE_API_SECRET=your_binance_secret

BYBIT_API_KEY=your_bybit_key
BYBIT_API_SECRET=your_bybit_secret

COINBASE_API_KEY=your_coinbase_key
COINBASE_API_SECRET=your_coinbase_secret

# Pushover (для уведомлений)
PUSHOVER_APP_TOKEN=your_pushover_token
```

## 🎯 Основные сценарии использования

### Сценарий 1: Создать сигнал через UI
1. Откройте Gradio (http://localhost:7860)
2. Перейдите на вкладку "Create Signal"
3. Заполните форму
4. Выберите "Also save to Google Sheets" если хотите дублировать в Sheets
5. Нажмите "Create Signal"
6. ✅ Сигнал создан и сохранен в DynamoDB (и опционально в Sheets)

### Сценарий 2: Просмотреть все сигналы
1. Откройте вкладку "View Signals"
2. Нажмите "Refresh" для обновления данных
3. Все сигналы из DynamoDB отобразятся в таблице

### Сценарий 3: Удалить сигнал
1. Скопируйте короткий ID из вкладки "View Signals"
2. Перейдите на вкладку "Delete Signal"
3. Вставьте ID (например: `a1b2c3d4...`)
4. Нажмите "Delete Signal"
5. ✅ Сигнал удален из DynamoDB

### Сценарий 4: Проверить текущую цену
1. Перейдите на вкладку "Check Price"
2. Выберите биржу (Binance, Bybit, Coinbase)
3. Введите символ (BTCUSDT)
4. Нажмите "Check Price"
5. Получите данные: цена, объем, время

### Сценарий 5: Массовая загрузка из Google Sheets
1. Добавьте сигналы в Google Sheets вручную
2. Откройте Gradio → вкладка "Sync from Sheets"
3. Нажмите "Sync from Google Sheets"
4. ✅ Все сигналы из Sheets загружены в DynamoDB

## 🌐 Публичный доступ (опционально)

### Локальная сеть
По умолчанию Gradio запускается на `0.0.0.0:7860`, доступен в локальной сети по IP:
```
http://192.168.x.x:7860
```

### Интернет (через Gradio Share)
Измените в `gradio_app.py`:
```python
app.launch(
    server_name="0.0.0.0",
    server_port=7860,
    share=True,  # ← Включите публичный доступ
    debug=True
)
```

Gradio создаст публичный URL на 72 часа:
```
Running on public URL: https://abc123.gradio.live
```

## 🔒 Безопасность

### Рекомендации:
1. **Не включайте `share=True`** для production без аутентификации
2. **Используйте Gradio Auth** если нужен публичный доступ:
   ```python
   app.launch(auth=("admin", "password123"))
   ```
3. **Ограничьте доступ по IP** через firewall
4. **Не коммитьте `.env`** в Git

## 🛠️ Архитектура

```
Gradio Web Interface (gradio_app.py)
│
├─ Services:
│  ├─ DynamoDBStorage     ← Основное хранилище
│  ├─ SheetsReader        ← Опциональная синхронизация
│  ├─ PriceChecker        ← Проверка цен с бирж
│  └─ ExchangeAdapters    ← Binance, Bybit, Coinbase
│
├─ Models:
│  └─ SignalTarget        ← Pydantic модели
│
└─ UI Tabs:
   ├─ Create Signal       ← Форма создания
   ├─ View Signals        ← Таблица просмотра
   ├─ Delete Signal       ← Удаление по ID
   ├─ Check Price         ← Живые цены с бирж
   └─ Sync from Sheets    ← Массовая загрузка
```

## 📊 Интеграция с Lambda

Gradio и Lambda **не конфликтуют**, а дополняют друг друга:

| Компонент | Назначение | Когда использовать |
|-----------|-----------|-------------------|
| **Lambda** | Автоматическая проверка сигналов по расписанию | Для фоновых задач, cron jobs |
| **Gradio** | Интерактивное управление сигналами | Для ручного управления, просмотра, создания |
| **Google Sheets** | Быстрое редактирование администраторами | Для быстрых правок админами |

**Workflow:**
1. Админ создает сигналы через **Gradio** → сохраняются в DynamoDB
2. Lambda по расписанию читает из DynamoDB → проверяет цены → отправляет уведомления
3. Пользователь может также управлять через **Google Sheets** → синхронизация в Gradio

## 🐛 Troubleshooting

### Ошибка: "DynamoDB connection failed"
```bash
# Проверьте AWS credentials
aws configure list

# Проверьте переменные окружения
echo %DYNAMODB_TABLE_NAME%
echo %DYNAMODB_REGION%
```

### Ошибка: "Google Sheets not initialized"
```bash
# Проверьте путь к credentials
ls secret-medium-476300-m9-c141e07c30ad.json

# Проверьте переменную окружения
echo %GOOGLE_SHEETS_CREDENTIALS_PATH%
```

### Ошибка: "No exchanges initialized"
```bash
# Проверьте API ключи бирж
echo %BINANCE_API_KEY%
echo %BYBIT_API_KEY%
echo %COINBASE_API_KEY%
```

### Gradio не запускается
```bash
# Переустановите Gradio
pip uninstall gradio
pip install gradio>=4.0.0

# Проверьте порт 7860 свободен
netstat -ano | findstr :7860
```

## 📚 Дополнительные материалы

- [Gradio Documentation](https://gradio.app/docs/)
- [AWS Lambda Deployment Guide](./DEPLOY_AWS_LAMBDA.md)
- [Google Sheets Integration](./docs/google_sheets_setup.md)

## 🎉 Готово!

Теперь у вас есть полноценный веб-интерфейс для управления торговыми сигналами! 🚀

**Запустите:**
```bash
run_gradio.bat
```

**Откройте:**
```
http://localhost:7860
```

**Наслаждайтесь!** 🎨
